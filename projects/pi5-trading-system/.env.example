# Pi5 Trading System - Go Implementation
# Environment Variables
#
# SECURITY: All sensitive values MUST be set via environment variables in production!
# Do NOT commit actual secrets to version control.

# ============================================================================
# REQUIRED IN PRODUCTION
# ============================================================================

# JWT Secret - Generate a strong random secret for production
# Example: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-here-min-32-chars

# Database Password
DB_PASSWORD=your-secure-database-password

# ============================================================================
# OPTIONAL (override config.yaml defaults)
# ============================================================================

# Server Configuration
# PI5_SERVER_PORT=8080
# PI5_SERVER_HOST=0.0.0.0

# CORS - Comma-separated list of allowed origins for production
# Example: CORS_ALLOWED_ORIGINS=https://trading.yourdomain.com,https://app.yourdomain.com
# CORS_ALLOWED_ORIGINS=*

# Database Connection
# DB_HOST=timescaledb
# DB_PORT=5432
# DB_USER=pi5trader
# DB_NAME=pi5_trading

# Redis Connection
# REDIS_HOST=redis
# REDIS_PORT=6379

# Logging
PI5_LOGGING_LEVEL=info
PI5_LOGGING_FORMAT=json  # json or console

# ============================================================================
# MARKET DATA - ALPACA (for real-time data)
# ============================================================================
# Get your free Alpaca API keys at: https://alpaca.markets
# Sign up for paper trading account (no real money required)
# Navigate to: https://app.alpaca.markets/paper/dashboard/overview
# Then: Account → API Keys → Generate New Key

# Alpaca API Key and Secret (REQUIRED for live market data)
# Leave blank to use simulated data (for testing without API keys)
ALPACA_API_KEY=your-alpaca-api-key-here
ALPACA_API_SECRET=your-alpaca-api-secret-here

# ============================================================================
# NOTES
# ============================================================================
#
# 1. Alpaca Free Tier:
#    - Real-time IEX market data (1-minute bars)
#    - Paper trading (virtual money)
#    - No credit card required
#    - Perfect for day trading development
#
# 2. To use simulated data (no API required):
#    - Leave ALPACA_API_KEY and ALPACA_API_SECRET blank
#    - System will automatically fall back to simulated data
#    - Or set provider: "simulated" in config.yaml
#
# 3. For paid data (optional upgrade):
#    - Alpaca SIP feed: $99/month for unlimited data
#    - Change feed_type: "sip" in config.yaml
#
# ============================================================================
