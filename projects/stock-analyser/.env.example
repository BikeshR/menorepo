# SimplyWall.st API Configuration
SIMPLYWALL_API_TOKEN=your_api_token_here

# Optional: Claude CLI Configuration
# If you need to use a custom Claude command path
# CLAUDE_COMMAND=/path/to/claude

# Logging Configuration (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO